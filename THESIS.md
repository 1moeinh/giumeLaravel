<div align="center">

![University Logo](https://upload.wikimedia.org/wikipedia/commons/thumb/c/c8/Shahab_Danesh_University_Logo.jpg/200px-Shahab_Danesh_University_Logo.jpg)

**دانشگاه شهاب دانش قم**

**دانشکده مهندسی کامپیوتر و فناوری اطلاعات**

<br><br><br>

**پایان‌نامه جهت دریافت درجه کارشناسی مهندسی کامپیوتر**

<br><br>

**موضوع:**

## طراحی و پیاده‌سازی سامانه فروش خدمات (گیومه)
## (Giuomeh Platform)

<br><br><br>

**استاد راهنما:**

**سرکار خانم اعظم فرخ**

<br><br>

**نگارش:**

**محمد معین حسن زاده**

<br><br>

**ورودی مهر ۱۳۹۸**

</div>

<div style="page-break-after: always;"></div>

# چکیده

پروژه پیش رو با عنوان **"گیومه" (Giuomeh)**، یک سامانه تحت وب به منظور ارائه و فروش خدمات آنلاین است. هدف اصلی این پروژه، ایجاد بستری مناسب برای معرفی خدمات، تعامل کاربران و مدیریت سفارشات می‌باشد.

در پیاده‌سازی این سامانه از تکنولوژی‌های مدرن توسعه وب استفاده شده است. بخش سمت سرور (Backend) با استفاده از فریم‌ورک قدرتمند **Laravel** (زبان PHP) پیاده‌سازی شده که امنیت و سرعت توسعه را تضمین می‌کند. برای طراحی رابط کاربری (Frontend) از فریم‌ورک **Bootstrap** و زبان JavaScript استفاده شده تا تجربه‌ای واکنش‌گرا (Responsive) و کاربرپسند برای کاربران فراهم شود.

معماری این پروژه بر اساس الگوی استاندارد **MVC (Model-View-Controller)** بنا شده است که موجب تفکیک لایه‌های منطقی، داده‌ای و نمایشی نرم‌افزار می‌گردد. از ویژگی‌های بارز این سامانه می‌توان به سیستم احراز هویت اختصاصی، پنل مدیریت مجزا برای ادمین، قابلیت ثبت سفارش (خرید خدمات)، و سیستم ثبت و تایید نظرات کاربران اشاره کرد.

**کلمات کلیدی:** فروشگاه اینترنتی، لاراول (Laravel)، معماری MVC، توسعه وب، مدیریت محتوا.

<div style="page-break-after: always;"></div>

# فصل اول: کلیات

## ۱-۱ مقدمه
امروزه اینترنت به بخشی جدایی‌ناپذیر از زندگی روزمره تبدیل شده است و تجارت الکترونیک (E-commerce) نقش بسزایی در اقتصاد جهانی ایفا می‌کند. وب‌سایت‌های پویا (Dynamic) که قابلیت تعامل با کاربر و مدیریت داده‌ها را دارند، جایگزین وب‌سایت‌های ایستای قدیمی شده‌اند. در این راستا، استفاده از فریم‌ورک‌های توسعه وب نظیر **Laravel** که بر پایه زبان PHP استوار هستند، به دلیل ساختار منظم، امنیت بالا و سرعت توسعه، بسیار محبوب شده است.

پروژه حاضر تلاشی برای پیاده‌سازی یک نمونه عملی از سیستم‌های فروش خدمات بر پایه این تکنولوژی‌ها می‌باشد.

## ۱-۲ تعریف پروژه (گیومه)
پروژه **"گیومه"** یک پلتفرم تحت وب برای معرفی و فروش خدمات است. در این سامانه، خدمات مختلف در دسته‌بندی‌های مشخص (به عنوان مثال خدمات آموزشی، فنی و ...) تعریف می‌شوند و کاربران پس از عضویت در سایت، می‌توانند این خدمات را مشاهده کرده، جزئیات آن‌ها را بررسی کنند و در صورت تمایل سفارش دهند.

همچنین امکان تعامل کاربران از طریق سیستم نظرات (Comments) فراهم شده است که با نظارت مدیر سیستم (Admin) منتشر می‌شوند.

## ۱-۳ اهداف پروژه
اهداف اصلی این پروژه عبارتند از:
1.  **پیاده‌سازی معماری MVC:** استفاده صحیح از الگوی Model-View-Controller برای سازماندهی کدها.
2.  **مدیریت کاربران:** ایجاد سطوح دسترسی متفاوت برای کاربران عادی و مدیران سیستم.
3.  **مدیریت محتوا:** قابلیت افزودن، ویرایش و حذف خدمات توسط مدیر.
4.  **تعامل کاربر:** امکان ثبت نظرات و پیگیری سفارشات.

## ۱-۴ ساختار کلی پایان‌نامه
در فصل دوم به تجزیه و تحلیل نیازمندی‌های سیستم خواهیم پرداخت. فصل سوم به طراحی پایگاه داده و دیاگرام‌های مربوطه اختصاص دارد. در فصل چهارم، جزئیات پیاده‌سازی و کدهای مهم بررسی می‌شوند و در نهایت در فصل پنجم، نحوه اجرای پروژه و نتیجه‌گیری ارائه خواهد شد.

<div style="page-break-after: always;"></div>

# فصل دوم: تجزیه و تحلیل سیستم

## ۲-۱ نیازمندی‌های سیستم
برای طراحی صحیح سیستم، ابتدا باید نیازمندی‌های عملکردی (Functional) و غیرعملکردی (Non-functional) آن را مشخص کنیم. این نیازمندی‌ها تعیین می‌کنند که سیستم چه کارهایی باید انجام دهد و چه ویژگی‌هایی داشته باشد.

### ۲-۱-۱ نیازمندی‌های عملکردی
این نیازمندی‌ها بر اساس نقش‌های موجود در سیستم (کاربر عادی و مدیر) تفکیک می‌شوند:

**الف) کاربر عادی (User/Member):**
*   **ثبت نام و ورود:** کاربر باید بتواند با وارد کردن نام، شماره تلفن و رمز عبور ثبت نام کند و وارد سیستم شود.
*   **مشاهده خدمات:** کاربر باید بتواند لیست خدمات موجود را مشاهده کند.
*   **مشاهده جزئیات خدمت:** امکان مشاهده صفحه اختصاصی هر خدمت.
*   **ثبت سفارش (خرید):** کاربر پس از ورود باید بتواند در خواست خرید یک خدمت را ثبت کند.
*   **ثبت نظر:** کاربر باید بتواند برای هر خدمت نظر خود را ثبت نماید (این نظر پس از تایید نمایش داده می‌شود).
*   **پنل کاربری:** مشاهده اطلاعات شخصی و تاریخچه سفارشات.

**ب) مدیر سیستم (Admin):**
*   **مدیریت خدمات:** مدیر باید بتواند خدمات جدید تعریف کند، قیمت یا نام آن‌ها را ویرایش کند و یا آن‌ها را حذف نماید.
*   **مدیریت کاربران:** مشاهده لیست تمام کاربران، حذف کاربر، و تغییر سطح دسترسی (ارتقاء به مدیر یا تنزل به کاربر عادی).
*   **مدیریت نظرات:** مشاهده نظرات تایید نشده و قابلیت تایید (Approve) یا حذف آن‌ها.
*   **مدیریت سفارشات:** مشاهده درخواست‌های ثبت شده کاربران.

### ۲-۱-۲ نیازمندی‌های غیرعملکردی
*   **امنیت:** دسترسی به صفحات مدیریتی و پنل کاربری باید محدود به کاربران احراز هویت شده باشد. رمز عبورها باید به صورت امن ذخیره شوند (در نسخه فعلی ساده ذخیره شده است اما قابل ارتقا است).
*   **رابط کاربری:** طراحی باید ساده و قابل فهم باشد (User Friendly) و از فریم‌ورک Bootstrap برای ظاهر آن استفاده شود.
*   **سرعت:** پاسخگویی سرور به درخواست‌ها باید در زمان معقول انجام شود.

## ۲-۲ سناریوهای کاربردی (Use Cases)

### ۲-۲-۱ سناریوی خرید خدمت
1.  کاربر وارد صفحه اصلی سایت می‌شود.
2.  از لیست خدمات، روی دکمه "اطلاعات بیشتر" یک خدمت کلیک می‌کند.
3.  اگر لاگین نکرده باشد، به صفحه ورود هدایت می‌شود.
4.  در صفحه جزئیات محصول، توضیحات و نظرات را مطالعه می‌کند.
5.  روی دکمه "درخواست سرویس" کلیک می‌کند.
6.  سیستم درخواست را در پایگاه داده ثبت کرده و کاربر را به لیست سرویس‌ها باز می‌گرداند.

### ۲-۲-۲ سناریوی تایید نظر
1.  کاربر در صفحه محصول نظری را ثبت می‌کند. سیستم این نظر را با وضعیت "تایید نشده" (submit = 0) ذخیره می‌کند.
2.  مدیر وارد پنل مدیریت می‌شود.
3.  در بخش "کامنت‌های تایید نشده"، نظر جدید را مشاهده می‌کند.
4.  مدیر دکمه "تایید" را می‌زند.
5.  سیستم وضعیت نظر را به "تایید شده" (submit = 1) تغییر می‌دهد و نظر در سایت نمایش داده می‌شود.

<div style="page-break-after: always;"></div>

# فصل سوم: طراحی سیستم و پایگاه داده

## ۳-۱ مقدمه
در این فصل به بررسی ساختار پایگاه داده (Database Schema) و موجودیت‌های سیستم می‌پردازیم. پایگاه داده این پروژه با استفاده از سیستم Migration لاراول طراحی شده است. جداول اصلی شامل `contacts` (کاربران)، `types` (خدمات)، `services` (سفارشات) و `comments` (نظرات) می‌باشد.

> **نکته:** نام‌گذاری جداول در کدهای پروژه با نام‌های استاندارد متفاوت است. در اینجا از نام‌های واقعی موجود در کد استفاده شده و نقش هر کدام توضیح داده می‌شود.

## ۳-۲ نمودار موجودیت-رابطه (ER Diagram)
ارتباط بین جداول به شرح زیر است:
*   هر **کاربر (Contact)** می‌تواند چندین **سفارش (Service)** داشته باشد (رابطه یک به چند).
*   هر **کاربر (Contact)** می‌تواند چندین **نظر (Comment)** ثبت کند (رابطه یک به چند).
*   هر **نوع خدمت (Type)** می‌تواند در چندین **سفارش (Service)** قرار گیرد (رابطه یک به چند).
*   هر **نوع خدمت (Type)** می‌تواند چندین **نظر (Comment)** داشته باشد (رابطه یک به چند).

## ۳-۳ معرفی جداول (Tables)

### ۳-۳-۱ جدول کاربران (`contacts`)
این جدول اطلاعات کاربران و مدیران سایت را نگهداری می‌کند.

| نام ستون | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInteger | شناسه یکتا (کلید اصلی) |
| `name` | String | نام و نام خانوادگی کاربر |
| `phone_number` | String | شماره تلفن (نام کاربری جهت ورود) |
| `password` | String | رمز عبور |
| `created_at` | Timestamp | تاریخ ایجاد |
| `updated_at` | Timestamp | تاریخ ویرایش |
| *`admin`* | *Integer/Boolean* | *ستون دستی (در مایگریشن نیست): 1=مدیر، 0=کاربر عادی* |

### ۳-۳-۲ جدول خدمات (`types`)
این جدول لیست خدماتی که در سایت فروخته می‌شوند را نگه می‌دارد.

| نام ستون | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInteger | شناسه یکتا (کلید اصلی) |
| `service_name` | String | نام خدمت |
| `service_price` | String | قیمت خدمت |
| `created_at` | Timestamp | تاریخ ایجاد |
| `updated_at` | Timestamp | تاریخ ویرایش |

### ۳-۳-۳ جدول سفارشات (`services`)
این جدول به عنوان جدول واسط، ارتباط بین کاربر و خدمت را برقرار می‌کند و نقش **سفارش (Order)** را ایفا می‌کند.

| نام ستون | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInteger | شناسه یکتا (کلید اصلی) |
| `contacts_id` | ForeignId | کلید خارجی به جدول `contacts` (کاربر) |
| `types_id` | ForeignId | کلید خارجی به جدول `types` (خدمت) |
| `created_at` | Timestamp | تاریخ ایجاد (تاریخ سفارش) |
| `updated_at` | Timestamp | تاریخ ویرایش |
| *`submit`* | *Integer/Boolean* | *ستون دستی (احتمالی): وضعیت سفارش* |

### ۳-۳-۴ جدول نظرات (`comments`)
این جدول نظرات کاربران در مورد خدمات را ذخیره می‌کند.

| نام ستون | نوع داده | توضیحات |
| :--- | :--- | :--- |
| `id` | BigInteger | شناسه یکتا (کلید اصلی) |
| `contacts_id` | ForeignId | کلید خارجی به جدول کاربران |
| `types_id` | ForeignId | کلید خارجی به جدول خدمات |
| `description` | Text | متن نظر |
| `submit` | Boolean | وضعیت تایید (0=در انتظار، 1=تایید شده) |
| `created_at` | Timestamp | تاریخ ثبت |
| `updated_at` | Timestamp | تاریخ ویرایش |

<div style="page-break-after: always;"></div>

# فصل چهارم: پیاده‌سازی

## ۴-۱ معماری سیستم (MVC)
در این پروژه از الگوی معماری **MVC (Model-View-Controller)** که هسته اصلی فریم‌ورک لاراول است، استفاده شده است:
*   **Model:** مسئول ارتباط با پایگاه داده است (فایل‌های موجود در `app/Models`).
*   **View:** رابط کاربری را نمایش می‌دهد (فایل‌های Blade در `resources/views`).
*   **Controller:** منطق برنامه را مدیریت می‌کند و بین Model و View ارتباط برقرار می‌کند (فایل‌های موجود در `app/Http/Controllers`).

## ۴-۲ مدل‌ها (Models)
مدل‌های اصلی استفاده شده عبارتند از:
*   `Contact`: مدل مربوط به کاربران.
*   `Type`: مدل مربوط به خدمات.
*   `Service`: مدل مربوط به سفارشات.
*   `Comment`: مدل مربوط به نظرات.

## ۴-۳ کنترلرها (Controllers)

### ۴-۳-۱ کنترلر اصلی (`giumeController`)
این کنترلر وظایف اصلی سایت مانند صفحه اصلی، ورود و خروج، و پنل‌های کاربری و مدیریت را بر عهده دارد.

**الف) منطق ورود (Login) و هدایت بر اساس نقش:**
در متد `check`، سیستم بررسی می‌کند که آیا کاربر مدیر است یا خیر و بر اساس آن ریدایرکت انجام می‌شود:

```php
public function check(Request $request){
    // جستجوی کاربر بر اساس شماره تلفن و رمز عبور
    $user = Contact::where('phone_number', $request->input('phone_number'))
                    ->where('password', $request->input('password'))
                    ->first();
    if($user){
        auth()->login($user); // لاگین کردن کاربر
        $admin = Auth::user()->admin; // بررسی نقش ادمین
        if ($admin){
            return redirect()->route('giume.admin'); // هدایت به پنل ادمین
        }else{
            return redirect()->route('giume.panel'); // هدایت به پنل کاربری
        }
    }
    else {
        return redirect()->route('giume.register'); // هدایت به ثبت نام در صورت عدم یافتن کاربر
    }
}
```

**ب) پنل مدیریت (Admin Panel):**
در متد `admin`، اطلاعات لازم برای داشبورد مدیریت جمع‌آوری می‌شود، از جمله لیست کاربران، سرویس‌ها و کامنت‌های تایید نشده:

```php
public function admin(){
    if(Auth::check()){
        $gate = Auth::user()->admin;
        if($gate){
            // دریافت تمام کاربران، سرویس‌ها و نظرات
            $member = Contact::all();
            $types = Type::all();
            $com = Comment::all();

            // فیلتر کردن کامنت‌های تایید نشده (submit == 0)
            $comment = [];
            foreach($com as $comm){
                if($comm->submit == 0){
                    $comment[] = $comm;
                    // ...
                }
            }
            return view('giume.admin', compact(['member','types', ...]));
        }
        // ...
    }
}
```

### ۴-۳-۲ کنترلر خدمات (`TypeController`)
این کنترلر وظیفه نمایش صفحه تکی هر خدمت و مدیریت ثبت نظرات برای آن را بر عهده دارد.

**نمایش خدمت:**
```php
public function show($id){
    $show = Type::find($id); // پیدا کردن سرویس با ID
    $com = Comment::all(); // دریافت همه نظرات

    // بارگذاری نظرات تایید شده (submit == 1) برای این سرویس
    $comment = [];
    foreach($com as $comm){
        if($comm->submit == 1 && $show->id === $comm->types_id){
            $comment[] = $comm;
        }
    }
    return view('giume.show', compact(['show','comment', ...]));
}
```

## ۴-۴ میان‌افزار (Middleware)
برای محافظت از روت‌ها (Routes) از یک میدل‌ور اختصاصی به نام `auth2` استفاده شده است. این لایه بررسی می‌کند که آیا کاربر لاگین کرده است یا خیر.

```php
public function handle(Request $request, Closure $next): Response
{
    if (!Auth::check()) {
        return redirect()->route('giume.login'); // اگر لاگین نیست، برو به صفحه ورود
    }
    return $next($request);
}
```

## ۴-۵ فرانت‌اند (Frontend)
در بخش فرانت‌اند از تکنولوژی‌های زیر استفاده شده است:
*   **Blade Engine:** موتور قالب‌ساز لاراول برای نمایش داده‌های پویا.
*   **Bootstrap:** فریم‌ورک CSS برای طراحی ریسپانسیو و زیبا.
*   **HTML/CSS:** برای ساختاردهی صفحات.

فایل‌های ویو (View) در پوشه `resources/views/giume` قرار دارند و شامل فایل‌هایی مانند `index.blade.php` (صفحه اصلی)، `admin.blade.php` (پنل مدیریت) و `show.blade.php` (صفحه محصول) می‌باشند.

<div style="page-break-after: always;"></div>

# فصل پنجم: آزمون و نتیجه‌گیری

## ۵-۱ اجرای پروژه
برای اجرای این پروژه روی سیستم محلی (Localhost)، مراحل زیر انجام می‌شود:

1.  **پیش‌نیازها:** نصب PHP، Composer و یک پایگاه داده مانند MySQL.
2.  **تنظیمات پایگاه داده:** اطلاعات اتصال به دیتابیس در فایل `.env` تنظیم می‌شود.
3.  **اجرای مایگریشن‌ها:** دستور `php artisan migrate` برای ایجاد جداول اجرا می‌شود.
4.  **اجرای سرور:** با دستور زیر سرور توسعه لاراول اجرا می‌شود:
    ```bash
    php artisan serve
    ```
5.  پس از اجرا، پروژه در آدرس `http://localhost:8000` قابل دسترسی است.

## ۵-۲ آزمون سیستم
*   **تست ورود:** با وارد کردن شماره همراه و رمز عبور صحیح، کاربر به پنل مربوطه هدایت می‌شود.
*   **تست خرید:** پس از لاگین، درخواست خرید یک سرویس ثبت شد و در جدول سفارشات (`services`) قابل مشاهده بود.
*   **تست نظردهی:** نظر ثبت شده ابتدا نمایش داده نشد، اما پس از تایید توسط ادمین در صفحه محصول ظاهر گردید.

## ۵-۳ پیشنهادات برای توسعه آینده
جهت بهبود و ارتقای این سامانه، موارد زیر پیشنهاد می‌گردد:
1.  **اتصال به درگاه پرداخت:** امکان پرداخت آنلاین هزینه خدمات.
2.  **هش کردن رمز عبور:** استفاده از الگوریتم‌های رمزنگاری مانند Bcrypt برای امنیت بیشتر رمز عبور کاربران.
3.  **بهبود پنل مدیریت:** افزودن نمودارهای آماری فروش و بازدید.
4.  **سیستم اطلاع‌رسانی:** ارسال پیامک یا ایمیل پس از ثبت سفارش.

## ۵-۴ نتیجه‌گیری
در این پایان‌نامه، مراحل طراحی و پیاده‌سازی سامانه فروش خدمات "گیومه" تشریح شد. این پروژه نشان داد که چگونه می‌توان با استفاده از فریم‌ورک لاراول و معماری MVC، یک سیستم قابل توسعه و مدیریت‌پذیر ایجاد کرد. تفکیک نقش‌های کاربری و مدیریت دقیق داده‌ها از نقاط قوت این پروژه محسوب می‌شود که تمامی اهداف اولیه تعیین شده را برآورده ساخته است.
